{
    "plugins": [
        "@semantic-release/github",
        "@semantic-release/release-notes-generator", 
        "@semantic-release/commit-analyzer",
        ["@semantic-release/changelog", {
            "changelogFile": "Assets/Mirror/Runtime/Transport/EpicOnlineTransport/CHANGELOG.md",
        }],
        ["@semantic-release/npm", {
            "npmPublish": false,
            "pkgRoot": "Assets/Mirror/Runtime/Transport/EpicOnlineTransport"
        }],
        ["@semantic-release/git", {
            "assets": ["Assets/Mirror/Runtime/Transport/EpicOnlineTransport/package.json", "Assets/Mirror/Runtime/Transport/EpicOnlineTransport/CHANGELOG.md"],
            "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
        }],
    ]
}
